rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /messages/{messageId} {
      // Allow anyone to read messages, but only authenticated users can create
      allow read: if true;
      allow create: if request.auth != null;
    }
  }
}
