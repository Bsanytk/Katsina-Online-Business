{
  "scripts": {
    "deploy-docs": "cd kob-react && npm install && npm run deploy:docs"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite-plugin-pwa": "^1.3.0"
  },
  "dependencies": {
    "browser-image-compression": "^2.0.2",
    "dotenv": "^17.3.1",
    "recharts": "^3.8.1"
  }
}
